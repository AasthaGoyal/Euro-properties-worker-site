import COLORS from './enumColors';

const EVENT_TASK = {
	COMPLETED: { name: 'Completed', color: COLORS.SUCCESS.name },
	PENDING: { name: 'Pending', color: COLORS.DANGER.name }
};
export default EVENT_TASK;
